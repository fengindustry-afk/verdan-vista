# Private app — keep the whole site out of search engines.
# Note: this prevents indexing, not access. The link is not secret; anyone
# who has the URL can still open it. Real protection is the login gate.
User-agent: *
Disallow: /
