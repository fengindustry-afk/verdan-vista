[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so client-side routes (e.g. /feedstock/FS-003) resolve to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
